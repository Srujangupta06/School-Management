import { Typography } from "@mui/material";

const Login = () => {
  return (
    <div>
      <center>
        <Typography variant="h5" component={'h5'}>Login Page</Typography>
      </center>
    </div>
  );
};

export default Login;
